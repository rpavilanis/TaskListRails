<h3>Welcome to Your Tasks</h3>

<% @tasks.each do |task| %>

<section>
  <ul>
    <li>
    <h4> <%= link_to(task[:title], show_path(task[:id])) %></h4>
    </li>
  </ul>
</section>

<% end %>

</form>
